{% extends "base1.html.twig" %}

{% block title %}Historique{% endblock %}

{% block contenu %}
<div class="div-social-top">
    <ul id="onglets">

        <li><a href="{{ path('display_compte') }}">Modifier le profil </a></li>
        <li><a href="{{ path('historique') }}"> Historique personnel </a></li>
        <li><a href="{{ path('mesoffres') }}"> Mes Offres </a></li>
        <li><a href="{{ path('historique') }}"> Mes Réservations</a></li>


    </ul>
</div>

<section class="simple-section">
    <div class="container">

<div class="portlet-body">
    <table class="table table-striped table-bordered table-hover" id="sample_1">
        <thead>
        <th></th>
        <th>Conducteur</th>
        <th>Passager 1</th>
        <th>Passager 2</th>
        <th>Passager 3</th>
        <th>Passager 4</th>
        <th>Ville de Départ</th>
        <th>Ville d'Arrivée</th>
        <th>Date et Heure</th>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <td> {{ reservation.getConducteur.prenom }} {{ reservation.getConducteur.nom }} </td>
            <td> {{ reservation.getgetPassager1.prenom }} {{ reservation.getPassager1.nom }} </td>
            <td> {{ reservation.getPassager2.prenom }} {{ reservation.getPassager2.nom }} </td>
            <td> {{ reservation.getPassager3.prenom }} {{ reservation.getPassager3.nom }} </td>
            <td> {{ reservation.getPassager4.prenom }} {{ reservation.getPassager4.nom }} </td>
            <td>{{ reservation.getVilleDep }}</td>
            <td>{{ reservation.getVilleArriv}}</td>
            <td>{{ reservation.date.format('Y-m-d') }} {{ reservation.heure.format("h:m:s") }}</td>


        {% endfor %}
        </tbody>
    </table>

</div></div></section>
{% endblock %}
